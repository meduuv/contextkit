[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "contextkit"
version = "0.1.0"
description = "Context composition helpers"
requires-python = ">=3.10"

[tool.hatch.build.targets.wheel]
packages = ["src/contextkit"]
